/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "plus.unsplash.com",
      "mir-s3-cdn-cf.behance.net",
      "images.unsplash.com",
      "cdn.rareblocks.xyz",
    ],
  },
};

export default nextConfig;
