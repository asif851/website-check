/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "plus.unsplash.com",
      "mir-s3-cdn-cf.behance.net",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
